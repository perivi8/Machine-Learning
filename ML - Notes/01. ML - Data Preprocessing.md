<------------------------------ ML-Data Preprocessing ------------------------------------------>

```python

-> Handling Imbalanced Values --16
-> Categorical Data  --17
-> Data Scalling  --18
-> Data Spliting --19





-> Handling Imbalanced Values


from sklearn.impute import SimpleImputer
imputer = SimpleImputer(missing_values=np.NaN , strategy='median')  # mean OR median
dff = imputer.fit_transform(df)      

<<-------------------------------------------------------------------------------------------------->>

-> Categorical Data  


1. In Categorical we can use Two methods
       
       * Label Encoder
       * One hot encoding


* Label Encoder (Recomended)

from sklearn.preprocessing import LabelEncoder
label_encoder = LabelEncoder()

df1['column_name'] = label_encoder.fit_transform(df1['column_name'])
df1['column_name']

=============================

* One Hot End=coding

a = pd.get_dummies(df2 , dtype=int)   # True = 1  , False = 0
a


<<-------------------------------------------------------------------------------------------------->>

-> Data Scalling  

1. In Data Scalling we have 2 types 

          * MinMax Scaler
          * Standard Scaler

* MinMax Scaler

from sklearn.preprocessing import MinMaxScaler
mx = MinMaxScaler()
MX = mx.fit_transform(df1)
MX

=============================

* Standard Scaler (Recomended)

from sklearn.preprocessing import StandardScaler
sc = StandardScaler()

x_train_sc = sc.fit_transform(x_train)
x_test_sc = sc.transform(x_test)

<<-------------------------------------------------------------------------------------------------->>

-> Data Spliting

from sklearn.model_selection import train_test_split
X_train , X_test , Y_train , Y_test = train_test_split(X,Y,test_size= 0.2, random_state=0 )








from sklearn.impute import SimpleImputer
from sklearn.preprocessing import LabelEncoder
from sklearn.preprocessing import MinMaxScaler
from sklearn.preprocessing import StandardScaler
from sklearn.model_selection import train_test_split